// Using the io library
using std::io;

// The main method
int main() {

    // Create new file stream and write a messsage
    FileStream fs = new FileStream("test.txt");
    fs << "Hello File from Å";

    // We remember to close it
    fs.close();

    // Return 0
    0;

}

// Call main
main();
