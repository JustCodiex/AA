// The main function to test...
int main() {

    // Create tuple
    (int, int) k = (5, 6);

    // Test tuple
    k match {
        case (5, v) => v * 11,
        case _ => 0
    };

};

// Call the function
main();
