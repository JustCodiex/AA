// The main function to test...
int main() {

    // Define the starting value
    var v = 0; // Integer inferred

    int i = v++; // 0

    int j = ++v; // 2

    println(i); // 0
    println(j); // 2

    ++i - j++; // -1

};

// Call the function
main();
