int main() {

    bool a = true;
    bool b = true;

    int r = 0;

    if (a || b) { // true
        r = r + 8;
    }

    if (a | b) { // true
        r = r + 29;
    }

    b = false;

    if (a || b) { // true
        r = r + 10;
    }

    if (a | b) { // true
        r = r + 8;
    }

    a = false;
    b = true;

    if (a | b) { // true
        r = r + 11;
    }

    r;

};

main();
